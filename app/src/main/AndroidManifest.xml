<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mahmoudashraf.neurologysystem">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activites.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activites.Register" />
        <activity android:name=".Activites.profile" />
        <activity android:name=".Activites.consulte_class" />
        <activity android:name=".Activites.homeAvtivity" />
        <activity android:name=".Activites.Admin_action" />
        <activity android:name=".Activites.explain"></activity>
    </application>

</manifest>